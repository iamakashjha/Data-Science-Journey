# Reflection: Dimensions, Shape, and Size

## Key Questions

### 1. What does `ndim` tell you?

### 2. What does `shape` tell you?

### 3. What does `size` tell you?

### 4. What is the difference between `(5,)` and `(5, 1)`?

### 5. Why must the number of elements remain constant during reshaping?

---

## Data Science Application

### 6. If `X.shape = (10000, 25)`, what does it mean?

### 7. Why is understanding `X.shape` important before training a model?

### 8. Where could incorrect array shapes cause problems?

---

## Quick Summary

Understanding array dimensions, shape, and size is essential for working with NumPy and building machine learning models. These attributes help you interpret data structure correctly and avoid errors when preparing features, reshaping arrays, and feeding data into models.

