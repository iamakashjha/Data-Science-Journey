## Data Science Connection

Imagine you're calculating averages for different departments.

Instead of:
```
calculate_average(math_scores)

calculate_average(science_scores)

calculate_average(english_scores)
```
You create one reusable function.

This is how **Data Scientists** build reusable data processing pipelines.